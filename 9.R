library(tidyverse)
library(readxl)
library(tidyr)
train<-read_excel(file.choose())
head(train)
summary(train)
dim(train)
str(train)
names(train)
glimpse(train)
train<-na.omit(train)
dim(train)
#train%>%drop_na
#dim(train)
library(lubridate)
train<-train%>%mutate(day=wday(Date_of_Journey,label=TRUE))
head(train)
